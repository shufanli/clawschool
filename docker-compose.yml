version: '3.8'

services:
  clawschool:
    build: .
    ports:
      - "8001:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: always
    logging:
      driver: json-file
      options:
        max-file: '3'
        max-size: 10m
